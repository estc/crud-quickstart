package ${package.Service};

import ${package.Entity}.${entity};
import cn.wnhyang.generator.pojo.PageResult;
import ${cfg.vo}.${entity}VO;

/**
 * $!{table.comment} 服务类
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${table.serviceName} {

    /**
     * 新建
     *
     * @param ${table.entityPath}VO 新建VO
     * @return id
     */
    Long create(${entity}VO ${table.entityPath}VO);

    /**
     * 更新
     *
     * @param ${table.entityPath}VO 更新VO
     */
    void update(${entity}VO ${table.entityPath}VO);

    /**
     * 删除
     *
     * @param id id
     */
    void delete(Long id);

    /**
     * 查询单个
     *
     * @param id id
     * @return po
     */
    ${entity} get(Long id);

    /**
     * 分页查询
     *
     * @param${table.entityPath}VO 分页VO
     * @return pageResult
     */
    PageResult<${entity}> page(${entity}VO ${table.entityPath}VO);

}
